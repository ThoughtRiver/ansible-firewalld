---
- hosts: localhost
  become_user: root
  roles:
    - ../../../ansible-firewalld
  vars:
    - firewalld:
        services:
            - service: http
              zone: test01
              immediate: yes
              permanent: False
              state: disabled
