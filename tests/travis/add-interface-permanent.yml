---
- hosts: localhost
  become_user: root
  roles:
    - ../../../ansible-firewalld
  vars:
    - firewalld:
        interfaces:
            - interface: eth902
              zone: test02
              immediate: no
              permanent: True
              state: enabled
